/*package com.g2inmobiliaria.app.Repositories;

import com.g2inmobiliaria.app.Entities.Person;
import org.springframework.data.jpa.repository.JpaRepository;

import java.util.List;

public interface PersonRepository extends JpaRepository<Person, Integer> {

    // Método para encontrar personas por apellido
    List<Person> findByFirstSurname(String firstSurname);

    // Método para encontrar personas por estado
    List<Person> findByStatus(boolean status);

}
*/